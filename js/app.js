/**
 * Inicialização da experiência.
 * Responsabilidades: conteúdo, navegação, cabeçalho e seção ativa.
 */
(function(){"use strict";const content=window.siteContent||{};const header=document.querySelector("[data-site-header]");const navLinks=Array.from(document.querySelectorAll("[data-nav-link]"));const sections=Array.from(document.querySelectorAll("[data-section]"));function applyContent(){document.querySelectorAll("[data-content]").forEach(element=>{const path=element.dataset.content.split(".");let value=content;path.forEach(key=>{value=value&&value[key]});if(typeof value==="string")element.textContent=value})}function updateHeader(){if(header)header.classList.toggle("is-scrolled",window.scrollY>24)}function updateActiveNav(activeId){navLinks.forEach(link=>{const active=link.getAttribute("href")==="#"+activeId;link.classList.toggle("is-active",active);if(active)link.setAttribute("aria-current","page");else link.removeAttribute("aria-current")})}function setupNavigation(){document.querySelectorAll("[data-scroll-link]").forEach(link=>{link.addEventListener("click",event=>{const target=document.querySelector(link.getAttribute("href"));if(!target)return;event.preventDefault();target.scrollIntoView({behavior:"smooth",block:"start"})})})}function setupSectionObserver(){if(!("IntersectionObserver"in window))return;const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)updateActiveNav(entry.target.id)})},{rootMargin:"-35% 0px -55% 0px",threshold:0});sections.forEach(section=>observer.observe(section))}function init(){applyContent();updateHeader();setupNavigation();setupSectionObserver();window.addEventListener("scroll",updateHeader,{passive:true})}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init,{once:true});else init()})();
